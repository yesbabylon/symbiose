# Inventory

The inventory package owns digital products, services, service models, service providers, installed software, software models, details, and access records.

Infrastructure-specific objects such as servers, instances, and services remain in the infra package and may reference inventory objects through relations.
